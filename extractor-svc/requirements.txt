fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.4
python-multipart==0.0.20
pdfplumber==0.11.4
PyMuPDF==1.25.1
# Camelot is optional (needs Ghostscript + tk). The pipeline degrades
# gracefully if it's missing; uncomment to enable table extraction.
# camelot-py[cv]==0.11.0
